# 百果园舆情监测系统 - 完整云端部署指南(小白版)

> 本指南专为非技术人员准备,从0到1详细说明如何将项目部署到阿里云服务器

**部署目标**: 在浏览器输入 `http://101.201.214.42:5000` 就能看到百果园舆情审核系统

---

## 📚 目录

1. [理解项目架构](#1-理解项目架构)
2. [云端资源准备](#2-云端资源准备)
3. [数据库部署方案](#3-数据库部署方案)
4. [代码部署步骤](#4-代码部署步骤)
5. [启动和访问](#5-启动和访问)
6. [常见问题](#6-常见问题)

---

## 1. 理解项目架构

### 1.1 百果园项目是什么?

这是一个**简化版**的舆情监测系统,专门为百果园定制:

```
用户访问网页 → 看到审核界面 → 查看舆情数据 → 人工审核标注
```

### 1.2 项目运行需要什么?

| 组件 | 作用 | 在哪里运行 |
|------|------|-----------|
| **Streamlit应用** | 提供网页审核界面 | 阿里云服务器 |
| **数据库** | 存储舆情数据 | 阿里云服务器(本地SQLite) 或 阿里云RDS |
| **Python环境** | 运行代码 | 阿里云服务器 |

### 1.3 核心文件说明

```
BettaFish-test/
├── baiguoyuan_review_app.py    # 主程序 - 审核界面
├── .env                         # 配置文件 - API密钥等
├── requirements.txt             # 依赖包列表
├── baiguoyuan_sentiment.db      # 本地数据库(SQLite)
└── 本文档
```

**关键**:
- `baiguoyuan_review_app.py` 是审核系统的入口
- `app.py` 是完整系统的入口(包含爬虫等功能,百果园项目暂不使用)

---

## 2. 云端资源准备

### 2.1 已有资源

✅ **阿里云ECS服务器**
- 公网IP: `101.201.214.42`
- 用户名: `root`
- 密码: `qwer.123`
- 系统: Ubuntu 22.04

### 2.2 需要开通的端口

登录 [阿里云控制台](https://ecs.console.aliyun.com/) → 安全组 → 配置规则:

| 端口 | 协议 | 授权对象 | 说明 |
|-----|------|---------|------|
| 5000 | TCP | 0.0.0.0/0 | Streamlit审核界面 |
| 22 | TCP | 0.0.0.0/0 | SSH远程登录(应该已开) |

**操作步骤**:
1. 找到你的ECS实例
2. 点击 "安全组" → "配置规则" → "添加入方向规则"
3. 端口范围: `5000/5000`
4. 协议: `TCP`
5. 授权对象: `0.0.0.0/0`
6. 点击"确定"

---

## 3. 数据库部署方案

### 🎯 推荐方案: 使用SQLite(最简单)

百果园项目**不需要**单独的MySQL/PostgreSQL数据库!

**为什么?**
- 当前代码已经包含 `baiguoyuan_sentiment.db` (SQLite数据库)
- SQLite是文件型数据库,无需安装配置
- 适合小规模数据(几千到几万条舆情)

**配置步骤**:

修改 `.env` 文件中的数据库配置:

```bash
# 使用SQLite(推荐)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=baiguoyuan2025
DB_NAME=baiguoyuan_sentiment
DB_CHARSET=utf8mb4
DB_DIALECT=sqlite   # 改为sqlite
```

或者在 `baiguoyuan_review_app.py` 中直接使用SQLite:

```python
# 第12行附近
DB_PATH = 'baiguoyuan_sentiment.db'  # 直接指定数据库文件
```

### 🔧 备选方案: 使用阿里云RDS(生产级)

如果未来数据量很大,可以升级到云数据库:

**步骤**:
1. 登录阿里云控制台 → 搜索 "RDS"
2. 创建PostgreSQL或MySQL实例(选择与ECS同地域:华北2-北京)
3. 创建数据库账号和数据库
4. 配置白名单(添加ECS内网IP)
5. 获取连接信息:
   - 内网地址: `rm-xxxxx.mysql.rds.aliyuncs.com`
   - 端口: `3306`(MySQL) 或 `5432`(PostgreSQL)
   - 用户名/密码

6. 更新 `.env`:
```bash
DB_HOST=rm-xxxxx.mysql.rds.aliyuncs.com
DB_PORT=3306
DB_USER=your_username
DB_PASSWORD=your_password
DB_NAME=baiguoyuan_sentiment
DB_DIALECT=mysql  # 或 postgresql
```

---

## 4. 代码部署步骤

### 4.1 登录服务器

**Mac/Linux**:
```bash
ssh root@101.201.214.42
# 输入密码: qwer.123
```

**Windows** (使用PowerShell或下载PuTTY):
```powershell
ssh root@101.201.214.42
# 输入密码: qwer.123
```

### 4.2 检查服务器环境

```bash
# 检查Python版本(需要3.9+)
python3 --version

# 检查目录
ls /root/
```

**预期结果**: 应该看到 `BettaFish-test` 目录(如果之前已上传)

### 4.3 上传代码到服务器

**方式1: 使用scp上传(Mac/Linux)**

在**本地电脑**打开终端:

```bash
cd /Users/xiaoyan/Desktop/舆情/BettaFish-test

# 打包项目(排除不必要文件)
tar czf baiguoyuan.tar.gz \
    --exclude='venv' \
    --exclude='venv311' \
    --exclude='__pycache__' \
    --exclude='*.pyc' \
    --exclude='.git' \
    --exclude='logs/*.log' \
    .

# 上传到服务器
scp baiguoyuan.tar.gz root@101.201.214.42:/root/

# 输入密码: qwer.123
```

**方式2: 使用sshpass(更快,需要先安装)**

```bash
cd /Users/xiaoyan/Desktop/舆情/BettaFish-test

# 安装sshpass (Mac)
brew install hudochenkov/sshpass/sshpass

# 一键上传
tar czf baiguoyuan.tar.gz --exclude='venv' --exclude='.git' .
sshpass -p 'qwer.123' scp baiguoyuan.tar.gz root@101.201.214.42:/root/
```

### 4.4 在服务器上解压和配置

回到**服务器SSH终端**:

```bash
# 进入目录
cd /root

# 如果之前有旧版本,先备份
if [ -d "BettaFish-test" ]; then
    mv BettaFish-test BettaFish-test.backup.$(date +%Y%m%d_%H%M%S)
fi

# 创建新目录并解压
mkdir -p BettaFish-test
tar xzf baiguoyuan.tar.gz -C BettaFish-test/
cd BettaFish-test

# 查看文件
ls -la
```

### 4.5 安装Python环境

```bash
cd /root/BettaFish-test

# 创建虚拟环境
python3 -m venv venv

# 激活虚拟环境
source venv/bin/activate

# 你会看到命令行前出现 (venv)

# 升级pip
pip install --upgrade pip

# 安装依赖(需要5-10分钟)
pip install streamlit pandas openai schedule requests beautifulsoup4
```

**如果要安装完整依赖**:
```bash
# 可选:安装所有依赖(包括爬虫等,较慢)
pip install -r requirements.txt
```

### 4.6 配置环境变量

检查 `.env` 文件是否存在:

```bash
ls -la .env
```

如果不存在:

```bash
cp .env.example .env
# 编辑配置
nano .env
```

**必须配置的内容**:

```bash
# 数据库(使用SQLite最简单)
DB_DIALECT=sqlite

# DeepSeek API密钥(用于AI分析)
INSIGHT_ENGINE_API_KEY=sk-aecac2abdacb49488c4fb1e7d1c62a94
MEDIA_ENGINE_API_KEY=sk-aecac2abdacb49488c4fb1e7d1c62a94
QUERY_ENGINE_API_KEY=sk-aecac2abdacb49488c4fb1e7d1c62a94
REPORT_ENGINE_API_KEY=sk-aecac2abdacb49488c4fb1e7d1c62a94
```

保存: `Ctrl+O` → `Enter` → `Ctrl+X`

### 4.7 上传数据库文件(如果本地有数据)

如果你的本地 `baiguoyuan_sentiment.db` 有测试数据,可以上传:

**在本地电脑**:
```bash
cd /Users/xiaoyan/Desktop/舆情/BettaFish-test
scp baiguoyuan_sentiment.db root@101.201.214.42:/root/BettaFish-test/
```

---

## 5. 启动和访问

### 5.1 启动服务

在**服务器SSH终端**:

```bash
cd /root/BettaFish-test
source venv/bin/activate

# 后台启动Streamlit审核界面
nohup streamlit run baiguoyuan_review_app.py \
    --server.port 5000 \
    --server.address 0.0.0.0 \
    --server.headless true \
    > streamlit.log 2>&1 &

# 记录进程ID
echo $! > streamlit.pid

echo "审核系统已启动!"
echo "访问地址: http://101.201.214.42:5000"
```

### 5.2 检查服务状态

```bash
# 查看进程
ps aux | grep streamlit

# 查看日志
tail -f streamlit.log

# 测试端口(应该看到输出)
curl -I http://localhost:5000
```

### 5.3 浏览器访问

在你的电脑浏览器打开:

**http://101.201.214.42:5000**

应该看到百果园舆情审核系统界面!

---

## 6. 常见问题

### ❌ 问题1: 浏览器无法访问

**原因**: 安全组端口未开放

**解决**:
1. 登录阿里云控制台
2. ECS → 安全组 → 配置规则
3. 添加入方向规则: 端口5000, TCP, 授权对象 0.0.0.0/0

### ❌ 问题2: 服务启动失败

**检查日志**:
```bash
tail -50 /root/BettaFish-test/streamlit.log
```

**常见错误**:
- `ModuleNotFoundError`: 依赖未安装 → `pip install streamlit`
- `Address already in use`: 端口被占用 → 杀掉旧进程
- `No such file or directory`: 文件路径错误 → 检查工作目录

### ❌ 问题3: 数据库连接失败

**使用SQLite**:
```bash
# 检查数据库文件
ls -la baiguoyuan_sentiment.db

# 如果不存在,创建空数据库
touch baiguoyuan_sentiment.db
```

**使用云数据库**:
```bash
# 测试连接(MySQL)
mysql -h rm-xxxxx.mysql.rds.aliyuncs.com -P 3306 -u username -p

# 测试连接(PostgreSQL)
psql -h rm-xxxxx.pg.rds.aliyuncs.com -p 5432 -U username -d baiguoyuan_sentiment
```

### ❌ 问题4: 端口被占用

```bash
# 查找占用5000端口的进程
lsof -i :5000
# 或
netstat -tlnp | grep 5000

# 杀掉进程
kill -9 <PID>
```

### ❌ 问题5: 服务停止了

**重启服务**:
```bash
# 停止旧进程
if [ -f /root/BettaFish-test/streamlit.pid ]; then
    kill $(cat /root/BettaFish-test/streamlit.pid)
fi

# 重新启动
cd /root/BettaFish-test
source venv/bin/activate
nohup streamlit run baiguoyuan_review_app.py \
    --server.port 5000 \
    --server.address 0.0.0.0 \
    --server.headless true \
    > streamlit.log 2>&1 &
echo $! > streamlit.pid
```

---

## 7. 服务管理命令速查

### 停止服务
```bash
kill $(cat /root/BettaFish-test/streamlit.pid)
rm /root/BettaFish-test/streamlit.pid
```

### 重启服务
```bash
cd /root/BettaFish-test
pkill -f "streamlit run baiguoyuan_review_app.py"
source venv/bin/activate
nohup streamlit run baiguoyuan_review_app.py \
    --server.port 5000 \
    --server.address 0.0.0.0 \
    --server.headless true \
    > streamlit.log 2>&1 &
echo $! > streamlit.pid
```

### 查看日志
```bash
# 实时查看
tail -f /root/BettaFish-test/streamlit.log

# 查看错误
grep -i error /root/BettaFish-test/streamlit.log
```

### 查看进程
```bash
ps aux | grep streamlit
```

---

## 8. 配置开机自启动(可选)

创建systemd服务:

```bash
cat > /etc/systemd/system/baiguoyuan.service << 'EOF'
[Unit]
Description=百果园舆情审核系统
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/BettaFish-test
Environment="PATH=/root/BettaFish-test/venv/bin"
ExecStart=/root/BettaFish-test/venv/bin/streamlit run baiguoyuan_review_app.py --server.port 5000 --server.address 0.0.0.0 --server.headless true
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
EOF

# 启用并启动服务
systemctl daemon-reload
systemctl enable baiguoyuan
systemctl start baiguoyuan

# 查看状态
systemctl status baiguoyuan
```

**管理命令**:
```bash
systemctl start baiguoyuan    # 启动
systemctl stop baiguoyuan     # 停止
systemctl restart baiguoyuan  # 重启
systemctl status baiguoyuan   # 状态
journalctl -u baiguoyuan -f   # 日志
```

---

## 9. 完整部署检查清单

- [ ] 阿里云安全组开放5000端口
- [ ] SSH登录服务器成功
- [ ] 代码已上传到 `/root/BettaFish-test/`
- [ ] Python虚拟环境已创建 (`venv/`)
- [ ] 依赖已安装 (`pip install streamlit pandas...`)
- [ ] `.env` 配置文件已正确配置
- [ ] 数据库可用(SQLite文件存在 或 云数据库可连接)
- [ ] Streamlit服务已启动
- [ ] 浏览器可访问 http://101.201.214.42:5000

---

## 10. 快速部署脚本

将以下内容保存为 `/root/deploy_baiguoyuan.sh`:

```bash
#!/bin/bash
set -e

echo "=== 百果园舆情系统一键部署脚本 ==="

cd /root/BettaFish-test

# 停止旧服务
echo "停止旧服务..."
pkill -f "streamlit run baiguoyuan_review_app.py" || true

# 激活环境
source venv/bin/activate

# 启动服务
echo "启动新服务..."
nohup streamlit run baiguoyuan_review_app.py \
    --server.port 5000 \
    --server.address 0.0.0.0 \
    --server.headless true \
    > streamlit.log 2>&1 &

echo $! > streamlit.pid

echo ""
echo "✅ 部署完成!"
echo "📝 访问地址: http://101.201.214.42:5000"
echo "📋 进程ID: $(cat streamlit.pid)"
echo "📊 查看日志: tail -f /root/BettaFish-test/streamlit.log"
```

**使用方法**:
```bash
chmod +x /root/deploy_baiguoyuan.sh
/root/deploy_baiguoyuan.sh
```

---

## 📞 技术支持

如果遇到问题:
1. 查看日志: `tail -f /root/BettaFish-test/streamlit.log`
2. 检查进程: `ps aux | grep streamlit`
3. 检查端口: `netstat -tlnp | grep 5000`
4. 保存错误信息截图

---

**最后更新**: 2025-11-07
**适用版本**: BettaFish-test 百果园定制版
